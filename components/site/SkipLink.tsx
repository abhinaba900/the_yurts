/**
 * Keyboard skip link. Off-screen until focused, then it sits over the header.
 */
export function SkipLink() {
  return (
    <a href="#main" className="u-skip-link">
      Skip to content
    </a>
  );
}
